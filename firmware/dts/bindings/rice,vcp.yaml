description: USB-to-UART driver

compatible: "rice,vcp"

include: base.yaml

properties:    
    uart:
      type: phandle
      required: true
      description: UART device to use for USB-to-UART device.
